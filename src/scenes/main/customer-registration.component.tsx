import React from "react";
import ContentView from "../../layouts/main/customer-registration";

export const CustomerRegistrationScreen = ({
  navigation,
}): React.ReactElement => <ContentView navigation={navigation} />;
